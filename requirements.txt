# KnowHow Tool — Abhängigkeiten
# Alles läuft lokal; es werden keine Cloud-SDKs verwendet.

fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
pydantic==2.13.4
python-multipart==0.0.32
watchdog==6.0.0
sqlite-vec==0.1.9

# Eigenes App-Fenster (WebView2) statt Systembrowser.
# Faellt automatisch auf Edge/Chrome im App-Modus zurueck, wenn es fehlt.
pywebview==6.2.1

# Anhänge: Text aus Dokumenten holen, Bilder und Scans aufbereiten
pypdf==6.16.1
pypdfium2==5.13.0
python-docx==1.2.0
pillow==12.3.0
